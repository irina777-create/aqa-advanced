const number=5;
for (let i = 1; i <= 10; i++) {
    console.log(`${number} × ${i} = ${number * i}`);
}

const number=5;
let count = 1;
while (count <= 10) {
    console.log(`${number} × ${count} = ${number * count}`);
    count++;
}

